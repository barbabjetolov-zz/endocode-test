node {    

    def image
    agent docker
    
    stage('Clone repository') {               

        checkout scm   
        sh "pwd"
        sh "ls"
    } 

    stage('Build image') {         
       
        image = docker.build("erizzardi/http-service")
    }
}