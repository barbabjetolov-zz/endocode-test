node {    

    def image

    stage('Clone repository') {               

        checkout scm   
        shell 'ls' 
    } 

    stage('Build image') {         
       
        image = docker.build("erizzardi/http-service")
    }
}