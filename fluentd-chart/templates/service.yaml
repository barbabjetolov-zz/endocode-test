apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluentd-chart.fullname" . }}
  labels:
    {{- include "fluentd-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluentd-chart.selectorLabels" . | nindent 4 }}
